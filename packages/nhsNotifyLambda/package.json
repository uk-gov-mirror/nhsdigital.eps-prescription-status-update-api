{
  "name": "nhsNotifyLambda",
  "version": "1.0.0",
  "description": "A lambda that processes notification requests off SQS",
  "main": "nhsNotifyLambda.js",
  "author": "NHS Digital",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "unit": "POWERTOOLS_DEV=true vitest run --config vitest.config.ts --coverage",
    "lint": "eslint  --max-warnings 0 --fix --config ../../eslint.config.mjs .",
    "compile": "tsc",
    "test": "npm run compile && npm run unit",
    "check-licenses": "license-checker --failOn GPL --failOn LGPL --start ../..",
    "cli:test-token": "npm run compile && node lib/cli/test-token-exchange.js"
  },
  "dependencies": {
    "@aws-lambda-powertools/commons": "^2.33.0",
    "@aws-lambda-powertools/logger": "^2.33.0",
    "@aws-lambda-powertools/parameters": "^2.33.0",
    "@aws-sdk/client-ssm": "^3.1037.0",
    "@middy/core": "^7.3.3",
    "@middy/input-output-logger": "^7.3.3",
    "@nhs/fhir-middy-error-handler": "^2.1.83",
    "axios": "^1.15.2",
    "axios-retry": "^4.5.0",
    "jose": "^6.2.2",
    "nock": "^15.0.0"
  },
  "devDependencies": {
    "@psu-common/testing": "^1.0.0",
    "axios-mock-adapter": "^2.1.0"
  }
}
